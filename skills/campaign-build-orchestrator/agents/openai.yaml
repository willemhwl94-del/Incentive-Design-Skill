interface:
  display_name: "Campaign Build Orchestrator"
  short_description: "Coordinate strategy, page, visual, and QA skills"
  brand_color: "#2563EB"
  default_prompt: "Use $campaign-build-orchestrator to turn this campaign idea into a structured multi-skill execution plan."
policy:
  allow_implicit_invocation: true
